apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: dev

spec:
  selector:
    app: frontend
    project: dev

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP
