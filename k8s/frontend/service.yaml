apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: production-devops

spec:
  selector:
    app: frontend
    project: production-devops

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP