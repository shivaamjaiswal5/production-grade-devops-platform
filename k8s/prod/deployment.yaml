apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend-deployment
  namespace: prod
  labels:
    app: frontend
    project: prod

spec:
  replicas: 2

  selector:
    matchLabels:
      app: frontend
      project: prod

  template:
    metadata:
      labels:
        app: frontend
        project: prod

    spec:
      containers:
        - name: frontend
          image: production-grade-devops-platform-frontend:latest
          imagePullPolicy: Never

          ports:
            - containerPort: 80
